import { SortPlacesPipe } from './sort-places.pipe';

describe('SortPlacesPipe', () => {
  it('create an instance', () => {
    const pipe = new SortPlacesPipe();
    expect(pipe).toBeTruthy();
  });
});
